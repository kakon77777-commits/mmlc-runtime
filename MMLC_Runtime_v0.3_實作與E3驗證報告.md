# MMLC Runtime v0.3：實作與 E3 驗證報告

**版本**：0.3.0  
**日期**：2026-07-21  
**研究階段**：E3——矩陣布局與 MMR 多方向執行  
**相容性**：保留 MMLF v0.1、v0.2 與 MMLC Runtime v0.2 的既有功能

---

## 摘要

MMLC Runtime v0.3 將前兩版仍偏向一維清單的 `layout`，升級為真正具有座標、空格、方向與相對引用語義的二維矩陣。

本版最重要的工作不是單純增加四種排序，而是正式區分兩類方向：

1. **展示方向**：只改變訪問、顯示與操作路徑，不得改變數學語義；
2. **執行方向**：當交易使用 `previous` 或 `next` 相對引用時，遍歷方向會改寫依賴圖與計算結果。

同時，本版加入固定座標引用 `left/right/up/down`。此類引用以矩陣空間為準，不隨掃描方向旋轉，因此可以檢驗「方向相對語義」與「空間絕對語義」是否被 Runtime 正確分離。

E3 隨機實驗得到：

- 64 個方向中立帳本，四方向語義不變準確率 1.0；
- 128 個方向敏感帳本，8,192 個格級結果全部符合預期；
- 64 個固定左鄰帳本，四方向語義不變準確率 1.0；
- 32×32 矩陣的 1,024 次已知列／欄路由中，訪問格數相對固定 row-major 降低約 95.77%。

最後一項僅證明**已知結構下的路由收益**，不代表 Runtime 已能自主發現正確方向。

---

## 1. 研究問題

E3 的核心問題可拆成四個：

1. Runtime 是否真正保存二維布局，而不只是把交易清單印成表格？
2. 水平與垂直遍歷是否具有正確的幾何順序？
3. Runtime 是否能區分「只改顯示」與「改變計算」的方向？
4. 明確方向與區域資訊是否能降低結構化查詢的訪問成本？

其中第 3 項最關鍵。若任何方向變化都會改變結果，代表系統混淆顯示與語義；若任何方向變化都不會改變結果，則方向尚未真正進入執行層。

---

## 2. 二維布局模型

對交易集合 $T$，v0.3 定義布局映射：

$$
\kappa:T\rightarrow\mathbb N\times\mathbb N,
$$

其中：

$$
\kappa(t)=(r_t,c_t)
$$

是交易 $t$ 的列與欄座標。

布局允許空格：

$$
L_{r,c}\in T\cup\{\varnothing\},
$$

但要求：

1. 每筆交易恰好出現一次；
2. 未知交易不得出現在布局；
3. 重複交易與遺漏交易均視為 schema／layout 錯誤。

若舊版程式直接建立 `MatrixLedger` 而沒有提供布局，v0.3 會將原 `display_order` 正規化成單列矩陣，以保持 v0.1／v0.2 相容性。

---

## 3. 六種物理遍歷

v0.3 實作：

- `left_to_right`；
- `right_to_left`；
- `top_to_bottom`；
- `bottom_to_top`；
- `snake_horizontal`；
- `snake_vertical`。

以布局：

```text
[a b c]
[d e f]
```

為例：

$$
\pi_{\rightarrow}=(a,b,c,d,e,f),
$$

$$
\pi_{\leftarrow}=(c,b,a,f,e,d),
$$

$$
\pi_{\downarrow}=(a,d,b,e,c,f),
$$

$$
\pi_{\uparrow}=(d,a,e,b,f,c).
$$

這修正了 v0.2 的限制：當時 `top_to_bottom` 與 `bottom_to_top` 尚未依真正欄座標產生路徑，只是沿扁平清單處理。

每條遍歷現在會記錄：

- 訪問交易；
- 每筆交易座標；
- 訪問格數；
- 曼哈頓路徑距離；
- 轉彎次數；
- 區域跨越次數；
- traversal role；
- 可重現時間戳。

---

## 4. 展示方向與執行方向

### 4.1 展示方向

若帳本交易互相獨立，方向僅是執行優先順序：

$$
\operatorname{Sem}(L,\pi_i)
=
\operatorname{Sem}(L,\pi_j).
$$

不同遍歷可有不同執行紀錄，但結果、稽核與依賴語義不變。

### 4.2 遍歷相對引用

v0.3 新增：

```yaml
base:
  matrix_ref: previous
  field: result
  default: 0
```

對活躍遍歷：

$$
\pi=(t_1,t_2,\ldots,t_n),
$$

`previous` 定義為：

$$
\operatorname{Prev}_{\pi}(t_i)=t_{i-1}.
$$

第一格若提供 `default`，則使用邊界值；否則產生明示錯誤。

此時依賴圖本身成為方向的函數：

$$
G=G(\pi).
$$

因此：

$$
\operatorname{Sem}(L,\pi_i)
\neq
\operatorname{Sem}(L,\pi_j)
$$

是合法且預期的。

### 4.3 固定空間引用

```yaml
base:
  matrix_ref: left
  field: result
```

固定左鄰定義為：

$$
\operatorname{Left}(t)=
\kappa^{-1}(r_t,c_t-1).
$$

它與掃描順序無關。即使使用 `right_to_left` 執行，拓撲排序仍會先執行真正的左鄰。

因此空間引用的語義是：

$$
G=G(\kappa),
$$

而不是：

$$
G=G(\pi).
$$

---

## 5. 雙雜湊

v0.3 將單一雜湊拆成兩層。

### 5.1 Semantic hash

包含：

- 計算結果；
- 稽核結果；
- 依賴圖；
- 資料通道；
- 污染；
- 根因；
- 全域帳本狀態。

它不直接包含純顯示順序。

### 5.2 Execution hash

另外包含：

- active execution traversal；
- execution order；
- layout；
- coordinates；
- operator lock。

所以可能出現：

$$
H_{sem}^{(1)}=H_{sem}^{(2)},
$$

但：

$$
H_{exec}^{(1)}\neq H_{exec}^{(2)}.
$$

這代表兩次執行得到相同語義，但經由不同矩陣路徑完成。

---

## 6. E3 測試設計

### 6.1 方向中立組

每個帳本為 4×4 獨立 identity 交易。四方向分別執行。

通過條件：

- 所有結果相同；
- 所有 semantic hash 相同；
- execution hash 應能區分四種執行路徑。

### 6.2 方向敏感組

每格執行：

$$
r_i=r_{\operatorname{previous}(i)}-a_i,
$$

邊界初值為 0。

不同遍歷會建立不同前驅鏈。實驗直接根據每條物理遍歷計算預期前綴結果，再逐格比較 Runtime 輸出。

### 6.3 固定空間組

每列第一格為常數，後續格執行：

$$
r_{r,c}=r_{r,c-1}+a_{r,c}.
$$

依賴固定指向左鄰。四方向執行應產生完全相同結果。

### 6.4 顯式路由組

建立 32×32 矩陣，查詢指定一列或一欄。

基準：固定 row-major，直到目標區域全部被訪問。  
MMR：查詢已提供區域與方向，直接只遍歷該列或欄。

此項測試的是：

> 在結構資訊已知時，顯式矩陣路由是否能減少不相關格的訪問。

它不測試路徑發現。

---

## 7. E3 結果

### 7.1 方向中立

| 指標 | 結果 |
|---|---:|
| 帳本數 | 64 |
| 帳本形狀 | 4×4 |
| 語義失敗 | 0 |
| 語義不變準確率 | 1.0 |
| 平均 execution hash 類別數 | 4.0 |

結果表示：純方向重排沒有污染數學語義，但執行軌跡仍被區分。

### 7.2 方向敏感掃描

| 指標 | 結果 |
|---|---:|
| 帳本數 | 128 |
| 帳本形狀 | 4×4 |
| 格級比較 | 8,192 |
| 結果錯誤 | 0 |
| 格級準確率 | 1.0 |
| 被判定為方向敏感 | 128／128 |
| 平均 semantic 類別數 | 4.0 |

結果表示 `previous` 已真正進入依賴與運算語義，而不是報告標籤。

### 7.3 固定空間引用

| 指標 | 結果 |
|---|---:|
| 帳本數 | 64 |
| 帳本形狀 | 4×4 |
| 方向語義失敗 | 0 |
| 方向不變準確率 | 1.0 |

結果表示固定左鄰與遍歷前一格已被正確區分。

### 7.4 路由成本

| 指標 | 結果 |
|---|---:|
| 矩陣 | 32×32 |
| 查詢 | 1,024 |
| 列查詢 | 524 |
| 欄查詢 | 500 |
| 固定 row-major 總訪問 | 775,179 |
| 顯式 MMR 總訪問 | 32,768 |
| 固定 row-major 平均訪問 | 757.01 |
| 顯式 MMR 平均訪問 | 32.00 |
| 訪問格降低 | 95.77% |

該結果的必要限定是：**查詢已提供目標列或欄。**

若系統不知道目標區域，仍需另外的索引、模型、規則或 Agent 來選擇路徑。

---

## 8. 完整回歸

v0.3 共有：

$$
22
$$

項自動測試，全部通過。

新增測試包括：

- 真二維四方向順序；
- 稀疏布局；
- 方向中立帳本；
- `previous` 方向敏感帳本；
- 固定左鄰方向不變帳本；
- 缺失鄰居且無 default；
- graph-only 模式拒絕 `previous`；
- 遍歷座標與路徑指標。

E0–E2 完整實驗亦重新執行：

| 階段 | 核心結果 |
|---|---|
| E0 | 512／512 竄改定位，0 FP，0 FN |
| E1 | 8,192／8,192 符號交換通過，負對照被抓出 |
| E2 | 根因與污染 precision／recall 全為 1.0 |
| E3 | 中立、敏感、空間三組全部符合預期 |

---

## 9. 實作中修正的問題

### 9.1 上下遍歷不得是扁平反轉

真實 `top_to_bottom` 必須逐欄訪問，而不是沿 row-major 清單前進。

### 9.2 相對引用必須先建立方向條件依賴圖

不能先用固定 DAG 排序，再在執行中臨時找 `previous`；否則引用目標可能尚未執行。

v0.3 的順序是：

```text
選定 traversal
→ 解出 MatrixRef
→ 建立 G(traversal)
→ 拓撲排序
→ 執行
```

### 9.3 固定空間引用不能被執行優先順序覆寫

`left` 指的是座標左鄰，不是執行清單中的前一筆。

### 9.4 舊版程式化帳本相容

E0–E2 實驗直接以 Python 建立 `MatrixLedger`。v0.3 透過 `__post_init__` 將其自動視為單列矩陣，避免升級破壞舊實驗。

---

## 10. 可確認與不可確認的結論

### 可以確認

$$
\boxed{
\text{MMLC 已具有真正的二維布局與方向條件執行語義。}
}
$$

並且：

1. 純展示方向不會無故改變語義；
2. 遍歷相對方向可以合法改變語義；
3. 固定空間方向不受掃描順序影響；
4. 執行路徑與數學語義可以分開雜湊；
5. 已知區域的顯式路由能減少不相關格訪問。

### 不能確認

目前不能宣稱：

- MMR 已普遍優於 AST、DAG、試算表或陣列語言；
- 方向在所有任務中都有價值；
- 95.77% 路由收益可外推到未知目標；
- Runtime 能自主判斷應使用哪一個方向；
- 循環、時間、固定點與 FDCS 已完成；
- 多向矩陣已成為新的通用計算複雜度模型。

---

## 11. 下一階段

建議 MMLC Runtime v0.4 進入兩條互相制衡的路線：

### E4-A：既有表示對照

- Python AST；
- SymPy expression DAG；
- 普通 JSON workflow；
- Excel 公式依賴圖；
- NetworkX DAG。

比較：表示成本、稽核資訊、方向資訊、根因追蹤、重現性與執行成本。

### E4-B：矩陣約束稽核

新增：

- 橫列規則；
- 直欄規則；
- 區塊規則；
- 橫直交叉矛盾；
- 最小修改集；
- 修復後再稽核。

完成 E4 後，才適合進入時間步、循環帳本與 FDCS 動態升級。

---

## 12. 重現方式

```bash
python -m pip install -e .
python -m pytest -q
bash run_tests.sh
```

E3 單獨執行：

```bash
python experiments/e3_matrix_directions/run_e3.py
```

方向比較：

```bash
python -m mmlc.cli compare-directions \
  examples/directional_previous_scan.yaml \
  --output outputs/directional_previous_scan
```

---

## 結論

MMLC Runtime v0.3 完成了一個關鍵分界：矩陣不再只是交易的顯示容器，而開始成為依賴解析與執行語義的一部分。

但這個分界不是由「多了四個方向名稱」構成，而是由三種可測情況共同建立：

$$
\boxed{
\text{方向中立}
\quad\neq\quad
\text{遍歷相對方向}
\quad\neq\quad
\text{固定空間方向}
}
$$

Runtime 能正確區分三者，才表示 MMR 已真正從視覺概念進入可執行形式系統。
