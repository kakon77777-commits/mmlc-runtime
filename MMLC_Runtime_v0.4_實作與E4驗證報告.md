# MMLC Runtime v0.4：實作與 E4 驗證報告

**版本**：0.4.0  
**日期**：2026-07-21  
**研究階段**：E4——橫列／直欄／區塊約束、跨軸矛盾與最小修復集  
**相容性**：保留 MMLF v0.1、v0.2、v0.3 與 Runtime E0–E3 功能

---

## 摘要

MMLC Runtime v0.4 將矩陣帳本從「每一格各自可計算、可稽核」推進為「整個矩陣可以承載跨格、跨列、跨欄與跨區塊約束」。

本版正式加入：

1. MMLF v0.4 `constraints`；
2. `row`、`column`、`block`、`region`、`cells` 五種作用域；
3. `sum_equals` 與 `all_equal` 約束；
4. 跨軸失敗約束的交點分析；
5. 格子—約束因子圖；
6. 對線性總和約束的最小支撐修復求解；
7. 多個等價修復方案的歧義保留；
8. 與普通扁平表格＋自訂約束程式的結果對照。

E4 隨機實驗結果：

- 128 個 6×6 單格錯誤案例，列／欄約束全部正確偵測；
- 128／128 由失敗列與失敗欄交點精確定位錯格；
- 128／128 得到唯一、正確的最小一格修復；
- 32 個同列正負抵消案例，列總和全部恢復正常，單看該列會漏報；
- 欄與 2×2 區塊約束仍在 32／32 案例中發現矛盾；
- 真實兩格修復組在 32／32 案例中都包含於最小方案集合；
- 32／32 正確保留多解歧義；
- 160／160 案例與獨立扁平表格參考實作結果完全一致。

最後一點非常重要：E4 沒有證明 MMLC 發明了新的約束數學。普通表格加上自訂程式同樣可以重現總和檢查。MMLC 的貢獻是把布局、約束、稽核、來源、方向、衝突交點、因子圖與修復方案整合進同一個可重現 Runtime。

---

## 1. 研究問題

E4 試圖回答四個問題：

1. 局部交易全部正確時，矩陣整體是否仍可能違反跨軸規則？
2. 單一軸的總帳被正負誤差抵消時，其他軸是否能揭露矛盾？
3. 多個失敗約束能否被轉換成最小格子修復問題？
4. 若修復不唯一，Runtime 能否保留歧義，而不是任意選擇一個故事？

令矩陣格集合為：

$$
X=\{x_1,x_2,\ldots,x_n\}.
$$

每條矩陣約束定義為：

$$
C_k=(S_k,f_k,y_k),
$$

其中：

- $S_k\subseteq X$ 是參與格子；
- $f_k$ 是約束函數；
- $y_k$ 是目標值。

對總和約束：

$$
f_k(X)=\sum_{x_i\in S_k}x_i,
$$

殘差為：

$$
r_k=f_k(X)-y_k.
$$

約束通過條件為：

$$
r_k=0.
$$

---

## 2. MMLF v0.4 約束格式

### 2.1 橫列約束

```yaml
constraints:
  - id: row-1
    kind: sum_equals
    field: result
    target: 15
    scope:
      type: row
      index: 1
```

### 2.2 直欄約束

```yaml
  - id: column-1
    kind: sum_equals
    target: 15
    scope:
      type: column
      index: 1
```

### 2.3 區塊約束

區塊採半開區間：

```yaml
  - id: block-top-left
    kind: sum_equals
    target: 14
    scope:
      type: block
      row_start: 0
      row_end: 2
      column_start: 0
      column_end: 2
```

這表示：

$$
0\le r<2,
\qquad
0\le c<2.
$$

### 2.4 自訂格與區域

```yaml
scope:
  type: cells
  ids: [a, d, f]
```

或：

```yaml
scope:
  type: region
  region: north-zone
```

### 2.5 約束欄位

目前支援：

- `result`：Runtime 計算結果；
- `audited_result`：帳本申報／被稽核結果。

這延續 v0.2 的資料通道區分：同一交易的計算值與申報值可以具有不同可信度。

---

## 3. 局部正確不代表矩陣正確

在 E4 的單格錯誤案例中，每一格都是合法的 identity 交易：

$$
x\mapsto x.
$$

因此所有格子的局部算子不變式都通過：

$$
S_i^{\mathrm{local}}=\mathrm{PASS}.
$$

但若中央格由 $5$ 改成 $8$，其所在列與欄分別出現：

$$
r_{\mathrm{row}}=3,
\qquad
r_{\mathrm{column}}=3.
$$

所以：

$$
\forall i,
S_i^{\mathrm{local}}=\mathrm{PASS}
$$

並不推出：

$$
S_{\mathrm{matrix}}=\mathrm{PASS}.
$$

這是 v0.4 最重要的語義擴張：

> 格子可以各自算對，但矩陣仍可能整體記錯。

---

## 4. 跨軸衝突圖

若兩條失敗約束來自不同軸，且共享格子：

$$
C_i\cap C_j\neq\varnothing,
$$

Runtime 會建立跨軸衝突記錄：

```json
{
  "constraints": ["column-1", "row-1"],
  "axes": ["column", "row"],
  "intersection": ["r1c1"]
}
```

對單格錯誤，失敗列與失敗欄的交集通常直接給出候選格：

$$
S_{\mathrm{row}}\cap S_{\mathrm{column}}
=\{x_{r,c}\}.
$$

但 Runtime 不把交點直接等同真因。交點只是約束證據的一部分；真正修復還需檢查其他通過約束是否會被破壞。

---

## 5. 格子—約束因子圖

v0.4 將矩陣約束編譯為二分因子圖：

$$
G_F=(V_X,V_C,E),
$$

其中：

- $V_X$：格子／交易節點；
- $V_C$：約束節點；
- $(x_i,C_k)\in E$ 當且僅當 $x_i\in S_k$。

以 3×3、三列加三欄約束為例：

- 變數節點：9；
- 約束節點：6；
- 邊：18。

此圖不是新的數學結構；它等價於 constraint factor graph／hypergraph 的常見表示。MMLC 的工作是把它與既有交易來源、座標、方向、稽核與持久化格式統一。

---

## 6. 最小支撐修復

### 6.1 問題定義

對每格加入修正量：

$$
\delta_i.
$$

對總和約束 $C_k$：

$$
\sum_{x_i\in S_k}\delta_i=-r_k.
$$

同時，原本通過的約束也必須保持通過：

$$
\sum_{x_i\in S_k}\delta_i=0
\qquad
(r_k=0).
$$

Runtime 尋找最小非零支撐：

$$
\min \|\delta\|_0,
$$

使全部可用線性總和約束同時成立。

### 6.2 實作方法

v0.4 採小規模精確枚舉：

1. 候選格取失敗約束成員聯集；
2. 依支撐大小 $1,2,3$ 逐層枚舉；
3. 使用 SymPy `linsolve` 解線性方程；
4. 驗證所有失敗與既有通過約束；
5. 找到第一個可行支撐大小後停止擴大；
6. 保留同一最小大小的全部方案，直到方案上限。

因此目前的「最小」是：

> 在設定的 `max_support` 範圍內，對線性 `sum_equals` 約束的精確最小支撐。

它不是大型稀疏最佳化器，也不是任意非線性約束的通用修復演算法。

### 6.3 唯一修復

中央格錯誤案例得到：

$$
\delta_{r1c1}=-3,
$$

修復後：

$$
8-3=5.
$$

所有列、欄約束同時恢復。

### 6.4 歧義修復

若只有一條列總和約束失敗：

$$
a+b+c=9,
\qquad
\text{target}=6,
$$

則以下三個修復都可成立：

$$
a\leftarrow a-3,
$$

$$
b\leftarrow b-3,
$$

$$
c\leftarrow c-3.
$$

Runtime 回傳三個方案並標記：

```text
ambiguous = true
```

它不會把數學上不足以辨識的問題偽裝成唯一真因。

---

## 7. 跨軸反抵消

考慮同一列兩格發生：

$$
+2,
\qquad
-2.
$$

則該列總和殘差為：

$$
(+2)+(-2)=0.
$$

單看列帳會通過。

但若兩格位於不同欄與不同區塊，則：

$$
r_{\mathrm{column\,A}}=+2,
$$

$$
r_{\mathrm{column\,B}}=-2,
$$

$$
r_{\mathrm{block\,A}}=+2,
$$

$$
r_{\mathrm{block\,B}}=-2.
$$

因此：

$$
\text{row PASS}
\not\Rightarrow
\text{matrix PASS}.
$$

這是 v0.1「局部誤差不可由全域有號總和抵消」在二維矩陣軸上的擴張。

---

## 8. 表示對照

v0.4 提供：

```bash
python -m mmlc.cli compare-representations \
  examples/cross_axis_single_error.yaml \
  --output outputs/representation_comparison
```

對照三種表示：

1. **MMLC MatrixLedger**：帶型別、來源、布局、約束與稽核；
2. **Flat table projection**：每格轉為普通列資料；
3. **Constraint factor graph**：格子節點、約束節點與二分邊。

獨立參考程式直接從 flat table 重算約束，結果與 MMLC 比較。

160 個隨機案例中：

$$
160/160
$$

約束狀態完全一致。

因此本版的誠實結論是：

> 普通表格加上足夠的自訂程式，可以重現相同的算術檢查。

MMLC 新增的不是「只有它能算」，而是：

- 約束正式進入資料格式；
- 座標作用域可直接編譯；
- 失敗約束形成跨軸衝突圖；
- 修復方案與歧義被標準化輸出；
- 同一結果自動進入 JSON、JSONL、Markdown、manifest 與 semantic hash；
- 可與 E0–E3 的來源、污染、根因與方向語義共存。

---

## 9. E4 實驗設計

### 9.1 單格錯誤組

- 帳本數：128；
- 矩陣：6×6；
- 每格：identity 交易；
- 約束：6 條列總和＋6 條欄總和；
- 每個帳本隨機竄改一格，誤差取 $\pm1$ 至 $\pm5$；
- 目標總和由乾淨矩陣生成。

通過條件：

1. 所有局部交易仍為 PASS；
2. 恰有一列與一欄失敗；
3. 交點等於真實錯格；
4. 最小修復支撐為 1；
5. 修復值等於乾淨值；
6. flat table 參考實作一致。

### 9.2 跨軸抵消組

- 帳本數：32；
- 矩陣：4×4；
- 約束：列、欄、2×2 區塊；
- 同一列兩格分別注入 $+\Delta$ 與 $-\Delta$；
- 兩格位於不同 2×2 區塊。

通過條件：

1. 被竄改列總和仍 PASS；
2. 兩欄與兩區塊失敗；
3. 真實兩格集合存在於最小修復方案；
4. 若另一列存在等價修復，必須保留歧義；
5. flat table 參考實作一致。

---

## 10. E4 結果

### 10.1 單格錯誤

| 指標 | 結果 |
|---|---:|
| 案例 | 128 |
| 每案例格數 | 36 |
| 局部交易偵測 | 0／128 |
| 跨軸約束偵測 | 128／128 |
| 列欄交點定位 | 128／128 |
| 唯一精確修復 | 128／128 |
| 表格參考一致 | 128／128 |
| 約束偵測準確率 | 1.0 |
| 精確修復準確率 | 1.0 |
| 平均枚舉支撐數 | 11.0 |

局部交易偵測為 0 不是失敗，而是實驗刻意設計：每格 identity 都算對，錯誤只存在於矩陣跨軸規則。

### 10.2 抵消案例

| 指標 | 結果 |
|---|---:|
| 案例 | 32 |
| 列總和隱藏錯誤 | 32／32 |
| 跨軸偵測 | 32／32 |
| 真實修復包含於最小方案 | 32／32 |
| 正確保留歧義 | 32／32 |
| 表格參考一致 | 32／32 |
| 偵測準確率 | 1.0 |
| 真實修復覆蓋率 | 1.0 |

### 10.3 表示等價

| 指標 | 結果 |
|---|---:|
| 總案例 | 160 |
| 與 flat table 參考一致 | 160 |
| 一致率 | 1.0 |

本次 E4 執行約 11.59 秒，約 13.80 個完整帳本案例／秒。此數值包含 Runtime 執行、約束評估、SymPy 最小支撐求解與表示對照，不應與單純交易吞吐量直接比較。

完整數據：`outputs/e4_cross_axis_constraints/metrics.json`。

---

## 11. 回歸結果

v0.4 共有：

$$
\boxed{29\text{ 項測試全部通過}}
$$

並重新執行 E0–E4：

| 階段 | 結果 |
|---|---|
| E0 算術與反抵消 | precision／recall 1.0；512 筆竄改全定位 |
| E1 符號—數值交換 | 8,192／8,192 通過；負對照成功失敗 |
| E2 根因追蹤 | 根因與污染 precision／recall 1.0 |
| E3 多方向執行 | 中立、敏感、固定空間全部符合預期 |
| E4 跨軸約束 | 單格修復與抵消偵測全部符合預期 |

---

## 12. 已知限制

### 12.1 修復求解器只支援線性總和

`all_equal` 可以被稽核，但目前會得到：

```text
repair_status = UNSUPPORTED
```

因為 v0.4 不宣稱已能對任意非線性或離散約束求最小修復。

### 12.2 最小支撐搜尋具有組合爆炸

候選格數為 $m$、最大支撐為 $k$ 時，最壞枚舉量約為：

$$
\sum_{i=1}^{k}\binom{m}{i}.
$$

目前適合小型矩陣、局部故障與研究驗證，不適合作為大型稀疏最佳化的最終實作。

### 12.3 約束不等於真實因果

列、欄與區塊約束是設計者給定的規則。Runtime 能指出哪些規則互相衝突，但不能僅憑規則證明某格就是現實世界中的真正原因。

### 12.4 多解不是缺陷

若帳本資訊不足，修復可能不唯一。`ambiguous=true` 是正確輸出，不應被強行消除。

### 12.5 表示等價不代表效能優越

E4 證明 MMLC 可與普通表格自訂程式得到相同結果，但尚未證明：

- 開發成本更低；
- 執行速度更快；
- 記憶體更省；
- 對大型工作流更容易維護；
- 普遍優於 spreadsheet、SQL constraint、factor graph 或 SMT。

---

## 13. 現階段結論

v0.4 可以支持以下有限但實質的結論：

$$
\boxed{
\text{MMLC 已能把矩陣跨軸規則轉換為可稽核、可交叉定位、可修復的帳本約束。}
}
$$

技術鏈現在是：

$$
\text{格級計算}
\rightarrow
\text{格級稽核}
\rightarrow
\text{符號交換}
\rightarrow
\text{依賴根因}
\rightarrow
\text{方向執行}
\rightarrow
\text{跨軸約束}
\rightarrow
\text{最小修復集}.
$$

但仍不能宣稱：

- 矩陣帳本是新的線性代數；
- 最小修復等於唯一真因；
- MMLC 已普遍優於既有表格、DAG 或 constraint solver；
- 動態時間、循環帳本與 FDCS 已完成。

---

## 14. 下一階段

MMLC Runtime v0.5 建議進入：

1. **時間索引帳本**：$L_t\rightarrow L_{t+1}$；
2. **延遲依賴**：引用前一期而非同一期；
3. **循環與固定點語義**：區分非法即時循環與合法迭代閉環；
4. **可逆修復交易**：修復不覆寫原值，而是新增補帳事件；
5. **干預分支**：比較修復前後與虛擬干預結果；
6. **FDCS 接口**：時間、語境、層級與方向權重正式進入狀態演化。

