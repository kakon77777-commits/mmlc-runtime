# MMLC Runtime v0.5 實作與 E5 驗證報告

**副題：時間索引、延遲依賴、宣告式固定點、不可變補帳與 FDCS 相容投影**

- 版本：MMLC Runtime 0.5.0
- MMLF：v0.1–v0.5 相容
- 日期：2026-07-22
- 階段：E5
- 狀態：可執行原型／合成實驗完成

---

## 摘要

MMLC Runtime v0.1–v0.4 已依序完成格級算術與反抵消稽核、符號—數值交換、依賴污染與根因追蹤、二維多方向執行，以及跨列／跨欄／區塊約束與最小修復。v0.5 將研究對象從靜態矩陣推進為離散時間矩陣帳本，正式加入五個構件：

1. 交易的時間索引與序列身分；
2. 以 lag 表達的延遲依賴；
3. 僅對明示群組開放的固定點迭代；
4. 不覆寫原交易的 append-only 補帳；
5. 將執行結果投影為 FDCS 節點、因果邊與時間衰減權重的接口。

E5 隨機驗證共執行 4,096 筆跨期交易、128 組收縮固定點、32 組發散負對照，以及 256 組雙階段補帳鏈。跨期值、固定點解析解、發散偵測、補帳原值保留、雜湊鏈與 FDCS 權重檢查均達 100% 實驗一致率。這些結果支持 MMLC 靜態帳本向離散動態帳本擴張的工程可行性，但不構成一般非線性固定點存在性、收斂性或 FDCS 完整因果推論能力的證明。

---

## 1. E5 的問題設定

v0.4 的交易圖是一個靜態 DAG。它可以表達同一批資料中的依賴、方向與跨軸約束，但尚不能自然回答：

- 某一格使用的是目前值，還是上一期值？
- 同一序列在不同時間點是否保持可追溯身分？
- 即時循環是否一律錯誤，還是部分循環其實代表合法平衡？
- 歷史錯帳應直接被改寫，還是追加修正紀錄？
- 如何將帳本圖輸出成 FDCS 所需的時間、語境與衰減因果結構？

E5 將以上問題拆成四個可反證命題。

### 命題 E5-1：離散延遲可執行

給定序列 $X$ 與時間 $t$，引用：

$$
X_{t-k}
$$

必須被解析成唯一交易，且執行順序不得違反時間依賴。

### 命題 E5-2：合法固定點與非法循環可分離

未宣告循環仍應拒絕；只有被列入固定點群組、具初值、容忍度與迭代上限的循環，才可進入數值求解。

### 命題 E5-3：補帳不等於改寫歷史

補帳必須形成新的有效視圖，同時保留原始申報值與每一次修正的前後狀態。

### 命題 E5-4：FDCS 投影必須誠實區分「已投影」與「已干預」

Runtime 可以輸出 FDCS 相容節點與權重，但若虛擬干預尚未執行，必須明確標記，而不能把介面宣告包裝為因果模擬結果。

---

## 2. MMLF v0.5 的時間原語

### 2.1 時間索引

每筆交易新增：

```yaml
time_index: 2
series_id: X
```

形成唯一鍵：

$$
\kappa_t(T_i)
=
(\operatorname{series}(T_i),\operatorname{time}(T_i)).
$$

對同一帳本，Runtime 要求每個 `(series_id,time_index)` 至多對應一筆交易。這使序列身分與交易顯示 ID 分離。

### 2.2 TemporalRef

```yaml
base:
  temporal_ref: X
  lag: 1
  field: result
```

在目前交易時間 $t$ 下，解析目標為：

$$
\operatorname{ResolveTemporal}(X,k,t)
=
\kappa_t^{-1}(X,t-k).
$$

`field` 保留 v0.2 的資料通道語義：

- `result`：Runtime 計算結果；
- `audited_result`：有效申報結果。

所以延遲依賴仍然是欄位敏感的，而不是只有交易級連線。

### 2.3 序列邊界

當 $t-k$ 不存在時有兩種合法行為。

#### 明示預設

```yaml
base:
  temporal_ref: X
  lag: 1
  default: 0
```

#### 明示錯誤

未提供 `default` 時，Runtime 產生缺失時間引用錯誤。它不會自動補零、前向填充或猜測初值。

---

## 3. 時間狀態快照

v0.5 新增：

```text
temporal_analysis.state_snapshots
```

對每一期 $t$ 保存：

$$
\mathcal S_t
=
\{(T_i,s_i,r_i,\hat r_i,q_i)\mid \operatorname{time}(T_i)=t\},
$$

其中：

- $s_i$：序列 ID；
- $r_i$：Runtime 計算值；
- $\hat r_i$：有效申報值；
- $q_i$：交易狀態。

這些快照是執行結果的一部分，不會回寫或覆蓋輸入文件。因此目前的「不可變」意義是：同一輸入與同一版本可重建相同快照，輸出檔案由持久化層追加或重新產生；它尚不是分散式不可變儲存。

---

## 4. 延遲依賴圖

對時間引用，Runtime 在普通依賴圖上增加帶 lag 的邊：

$$
e_{ij}
=
(T_i\rightarrow T_j,\Delta t,\text{field},\text{channel}).
$$

其中：

$$
\Delta t
=
t_j-t_i.
$$

E5 要求所有由 `TemporalRef` 產生的邊滿足：

$$
\Delta t\ge 0.
$$

一般跨期鏈仍是 DAG，因此沿用 v0.2 的污染傳播與根因追蹤。若上一期交易的引用通道不可信，下一期會標記 `TAINTED`；若下一期自身帳本也錯誤，則標記 `FAIL` 或 `ERROR`。

---

## 5. 固定點群組

### 5.1 為什麼不能直接允許所有循環

若 Runtime 遇到：

$$
A\rightarrow B\rightarrow A,
$$

而沒有求解語義，直接執行會造成未定義順序。v0.1–v0.4 因此拒絕所有循環。

v0.5 保留這個預設，只新增明示例外：

```yaml
fixed_point_groups:
  - id: xy-equilibrium
    members: [x, y]
    method: jacobi
    tolerance: 1.0e-13
    max_iterations: 200
    initial_values: {x: 0, y: 0}
```

沒有被群組包覆的循環仍產生 `DependencyCycleError`。

### 5.2 群組壓縮

令固定點群組為 $G_k$。Runtime 將群內交易壓縮成一個執行單元：

$$
\widetilde G
=
(V/G_k,E/G_k).
$$

先對壓縮圖做拓撲排序；群組的外部父節點完成後，再進入群內迭代。這避免「先迭代一個群組，後來才發現它還依賴未完成外部交易」的錯誤。

### 5.3 Jacobi 更新

設群內狀態為：

$$
\mathbf x^{(n)}.
$$

同步更新：

$$
\mathbf x^{(n+1)}
=
F(\mathbf x^{(n)}).
$$

所有群內引用在同一輪都讀取舊狀態，而不是讀到同輪已更新值。

收斂指標使用 scaled delta：

$$
d_n
=
\max_i
\frac{|x_i^{(n+1)}-x_i^{(n)}|}
{1+|x_i^{(n+1)}|+|x_i^{(n)}|}.
$$

若：

$$
d_n\le\varepsilon,
$$

則標記收斂。

### 5.4 最小正例

新增 `affine` 算子：

$$
\operatorname{Affine}(b,a;s)=sb+a.
$$

測試：

$$
x=0.5y+1,
$$

$$
y=0.5x+1.
$$

解析解：

$$
x=y=2.
$$

從 $(0,0)$ 出發，v0.5 會在容忍度內收斂，並把迭代次數與最終 delta 寫入交易結果及群組分析。

### 5.5 負對照

測試：

$$
x=1.1y+1,
$$

$$
y=1.1x+1.
$$

在指定初值與 20 次上限下不收斂。Runtime 回傳：

```text
FixedPointConvergenceError
```

並將全域帳本標記為 FAIL。

這裡的結論僅是：Runtime 能忠實執行一個指定迭代協議並偵測未達容忍度。它沒有證明所有 $|ac|<1$ 以外的系統發散，也沒有做 Banach 條件的符號證明。

---

## 6. 不可變補帳

### 6.1 問題

若歷史交易申報：

$$
3\times2=7,
$$

直接把 `7` 改成 `6` 會摧毀「當時究竟申報了什麼」的證據。

### 6.2 v0.5 補帳模型

```yaml
corrections:
  - id: correction-001
    target_tx_id: multiply-claim
    field: declared_result
    mode: replace
    value: 6
    reason: verified correction
```

Runtime 建立兩個並存值：

$$
r_i^{\mathrm{original}},
$$

$$
r_i^{\mathrm{effective}}.
$$

原始 `Transaction` 物件不被修改；稽核使用依序套用修正後的有效視圖。

### 6.3 Replace 與 Delta

v0.5 支援：

#### Replace

$$
r'\leftarrow v.
$$

#### Delta

$$
r'\leftarrow r+\Delta.
$$

目前補帳只作用於 `declared_result`／`expected_result`。不允許藉補帳重寫算子、依賴圖、時間或來源身分。 補帳值不得引入新的交易、矩陣或時間引用。

### 6.4 雜湊鏈

第一筆修正前置雜湊：

$$
h_0=00\ldots0.
$$

第 $k$ 筆：

$$
h_k
=
\operatorname{SHA256}
(\operatorname{CanonicalJSON}
(h_{k-1},c_k,b_k,a_k)).
$$

其中：

- $c_k$：修正內容；
- $b_k$：修正前值；
- $a_k$：修正後值。

輸出保存每筆 `previous_hash`、`entry_hash` 與最終 `head_hash`。

這個機制提供順序敏感、可重現的單機稽核鏈，但不提供共識、簽章、權限管理或抗刪除儲存。因此不得稱為區塊鏈。

---

## 7. FDCS 相容投影

### 7.1 節點

每筆交易投影為：

$$
N_i
=
(T_i,s_i,t_i,x_i,q_i,c_i,l_i),
$$

其中：

- $s_i$：序列；
- $t_i$：時間；
- $x_i$：狀態；
- $q_i$：帳本健康狀態；
- $c_i$：語境；
- $l_i$：預留分形層級。

### 7.2 因果邊

依賴邊投影為：

$$
E_{ij}
=
(N_i,N_j,\Delta t,w^0,w^{\mathrm{eff}},\text{channel}).
$$

有效權重：

$$
w^{\mathrm{eff}}
=
w^0\lambda^{\Delta t}M(c).
$$

目前：

- $w^0$ 可由子交易 `context.causal_weight` 指定，預設 1；
- $\lambda$ 由 `fdcs.decay_lambda` 指定；
- $M(c)$ 目前是一個明示純量 `context_modulation`；
- 同期邊的 $\Delta t=0$；
- 延遲邊的 $\Delta t>0$。

### 7.3 虛擬干預邊界

輸入可宣告：

```yaml
interventions:
  - target: x-t1
    value: 100
```

但 v0.5 輸出：

```text
DECLARED_NOT_EXECUTED
```

這表示介面已為下一版保留，但目前不會建立反事實分支、切斷傳入因果或重算後代。

---

## 8. 自動測試

v0.5 共通過：

$$
\boxed{35\text{ 項測試}}
$$

新增 E5 測試包括：

1. 跨期鏈與 FDCS 權重；
2. 序列邊界 default；
3. 收縮固定點收斂；
4. 發散固定點明示錯誤；
5. 未宣告循環仍拒絕；
6. 補帳原值保留與雜湊鏈。

E0–E4 的 29 項回歸全部保留。

---

## 9. E5 隨機實驗

實驗種子：

```text
20260722
```

完整數據：

```text
outputs/e5_temporal_dynamics/metrics.json
```

### 9.1 時間鏈

設置：

- 64 個帳本；
- 每個帳本 64 期；
- 每期：

$$
X_t=X_{t-1}+u_t;
$$

- $u_t$ 為隨機整數；
- FDCS 衰減 $\lambda=0.93$。

結果：

| 指標 | 結果 |
|---|---:|
| 帳本 | 64 |
| 每帳本期數 | 64 |
| 交易總數 | 4,096 |
| 正確交易 | 4,096 |
| 準確率 | 1.0 |
| FDCS 權重檢查 | 4,032 |
| 權重錯誤 | 0 |
| 本次吞吐 | 約 8,551 筆／秒 |

### 9.2 隨機收縮固定點

每組：

$$
x=ay+b,
$$

$$
y=cx+d,
$$

其中 $a,c\in[-0.8,0.8]$。解析解：

$$
x^*=\frac{ad+b}{1-ac},
$$

$$
y^*=cx^*+d.
$$

結果：

| 指標 | 結果 |
|---|---:|
| 收縮案例 | 128 |
| 成功收斂 | 128 |
| 與解析解一致 | 128 |
| 準確率 | 1.0 |
| 平均迭代 | 30.8672 |
| 最大觀察迭代 | 110 |

### 9.3 發散負對照

$a,c$ 取 1.05–1.30。

| 指標 | 結果 |
|---|---:|
| 發散案例 | 32 |
| 正確偵測 | 32 |
| 偵測率 | 1.0 |

### 9.4 補帳鏈

每個案例產生一筆錯誤申報、一次暫定修正與一次最終修正。

| 指標 | 結果 |
|---|---:|
| 案例 | 256 |
| 最終稽核通過 | 256 |
| 原始值保留 | 256 |
| 重跑 head hash 一致 | 256 |
| chain link 正確 | 256 |

---

## 10. 這次實作中確認的架構結論

### 10.1 時間不是顯示欄，而是依賴解析的一部分

`time_index` 若只放在報告中，無法改變依賴。v0.5 把時間索引加入 `TemporalRef` 解析，因此它會改變拓撲邊與執行順序。

### 10.2 循環需要顯式求解契約

「允許循環」本身沒有意義。固定點群組至少需要：

- 成員；
- 方法；
- 初值；
- 容忍度；
- 最大迭代；
- 不收斂處理。

### 10.3 補帳屬於視圖生成，不屬於歷史覆寫

有效申報值可以改變，但原始申報值與修正鏈必須共同存在。這使帳本的「現在正確值」與「歷史曾經聲稱什麼」可以同時被查詢。

### 10.4 FDCS 接口必須晚於決定性帳本

FDCS 的語境、動態因果與干預比靜態算術更容易產生不可檢驗敘述。v0.5 只在 MMLC 已有依賴圖、時間與狀態後投影 FDCS 結構，並保留干預未執行的明示邊界。

---

## 11. 已知限制

1. 時間只支援非負整數索引，尚無連續時間、日期時區或不規則採樣語義。
2. `TemporalRef` 目前依靠完全匹配 `(series_id,t-lag)`，尚無 nearest、window、aggregation。
3. 固定點只支援 Jacobi，同期群組內只有單一狀態通道。
4. 數值收斂不能取代形式收斂條件。
5. 浮點固定點仍受容忍度、尺度與初值影響。
6. 補帳只支援申報結果，不支援重寫結構。
7. 雜湊鏈沒有簽章、權限、遠端見證或不可刪除保證。
8. FDCS 只做投影，沒有反事實求解。
9. 語境調製目前是外部給定純量，不是學得函數。
10. 分形層級目前只是欄位，不會自動生成遞歸拓撲。
11. 尚未與 Excel iterative calculation、SimPy、dataflow engine、SMT/constraint solver 進行公平效能對照。

---

## 12. E5 判定

可確認：

$$
\boxed{
\text{MMLC 靜態矩陣帳本可以擴張為具離散時間、延遲邊與宣告固定點的動態帳本。}
}
$$

亦可確認：

$$
\boxed{
\text{歷史補帳可以不覆寫原申報，並形成決定性稽核鏈。}
}
$$

但只能說：

$$
\boxed{
\text{MMLC 已建立 FDCS 的資料接口，尚未完成 FDCS 的干預與動態因果求解。}
}
$$

---

## 13. 下一階段：v0.6／E6

建議順序：

1. **虛擬干預分支**：固定目標、切斷傳入邊、保留傳出邊，建立 baseline／intervention 雙帳本。
2. **多語境並行**：同一時間點在多個語境中生成平行狀態。
3. **方向不對稱因果權重**：向下、向上與橫向使用不同衰減。
4. **分形層級索引**：將層級路徑編入節點身分與邊權重。
5. **時間窗口約束**：rolling sum、rolling conservation、window anomaly。
6. **固定點插件**：Gauss–Seidel、damped iteration、Newton 或外部 solver。
7. **形式條件**：對 affine 群組計算譜半徑或收縮上界，區分「被測得收斂」與「可預先保證收斂」。
8. **對照實驗**：與 spreadsheet iterative calculation、普通 DAG、NumPy/SciPy solver 比較正確率、可追溯性與成本。

---

## 結語

v0.5 的真正改變不是多了 `time_index`，而是矩陣帳本開始同時保存三種順序：

$$
\text{空間位置},
$$

$$
\text{依賴順序},
$$

$$
\text{時間順序}.
$$

它們可以一致，也可以彼此不同。延遲引用將過去狀態帶入現在；固定點群組讓少數被明確規範的循環成為可求解平衡；補帳鏈則讓現在的修正不必抹掉過去的錯誤。至此，MMLC 已從靜態可稽核矩陣，推進為第一版可追溯動態帳本。
