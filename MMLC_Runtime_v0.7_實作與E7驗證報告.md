# MMLC Runtime v0.7：軟干預、循環重求解、分支差分帳與有限觀測可識別性

**文件代號：** EML-MMLC-2026-R07  
**版本：** v0.7.0  
**日期：** 2026-07-22  
**作者：** Neo.K 與 Aletheia（GPT-5.6 Thinking）  
**所屬：** EveMissLab

---

## 摘要

MMLC Runtime v0.6 已能對無循環帳本執行硬式 `do_set` 干預、切斷目標節點的傳入邊，並在多語境中重算後代。v0.7 進一步處理四個尚未閉合的問題：

1. 並非所有干預都應切斷因果父節點；
2. 干預目標可能位於固定點循環內；
3. 反事實分支的差異需要可稽核、可重跑的帳本；
4. 多個干預可能互相衝突，或在有限觀測下無法區分。

本版新增仿射軟干預、固定點群內硬／軟干預重求解、分支差分雜湊鏈、干預衝突與冗餘稽核，以及指定觀測交易上的分支可區分性分析。49 項自動測試全部通過；E7 隨機實驗完成 2,048 個軟干預交易值、128 條固定點干預分支、256 組衝突／冗餘案例與 64 組有限觀測可識別性案例，所設計指標皆為 1.0。

本報告的結論是：MMLC 已能在已知、決定性、離散的結構帳本中，區分硬干預與軟干預，重新求解已宣告的收縮循環，並保存分支差異與有限觀測等價類。但這不等於一般統計因果識別、任意非線性循環求解或未知因果圖發現。

---

## 1. 研究問題

v0.6 的硬干預可寫成：

\[
do(X_k=x^*)
\]

其語義是移除所有：

\[
Pa(X_k)\rightarrow X_k
\]

並將目標值固定為 \(x^*\)。這適合模擬外部強制設定，但無法表示下列情況：

- 政策只改變反應幅度，沒有消除原有輸入；
- 參數偏移、增益調整或偏置修正；
- 干預發生在互相依賴的固定點群內；
- 兩個不同干預在有限觀測格上產生相同結果；
- 同一語境對同一節點宣告互不相容的干預。

E7 因此要驗證：

> 能否把硬干預、軟干預、循環重求解、差分證據與分支可區分性統一到同一套矩陣帳本執行語義中？

---

## 2. 硬干預與軟干預

### 2.1 硬干預

對原方程：

\[
X=F(Pa(X)),
\]

硬干預：

\[
do(X=x^*)
\]

將其改寫為：

\[
X=x^*,
\]

並切斷父節點到 \(X\) 的傳入邊。

在 Runtime 中：

```yaml
kind: do_set
target_tx_id: root
value: 10
```

硬干預的契約包括：

- 目標結果等於指定值；
- 傳入依賴邊被記錄為 cut edge；
- 目標後代依原結構重新計算；
- 祖先與非後代保持不變。

### 2.2 仿射軟干預

軟干預保留父節點，修改結構方程的輸出：

\[
F'(Pa(X))=aF(Pa(X))+b.
\]

MMLF v0.7 表示為：

```yaml
kind: soft_affine
target_tx_id: root
scale: 2
shift: 1
```

並提供兩個語法糖：

\[
\texttt{soft\_shift}(b)
\equiv
\texttt{soft\_affine}(1,b),
\]

\[
\texttt{soft\_scale}(a)
\equiv
\texttt{soft\_affine}(a,0).
\]

軟干預的關鍵不變式是：

\[
Pa(X)\rightarrow X
\]

仍存在。也就是：

\[
\boxed{\text{修改方程}\neq\text{切斷方程的輸入來源}}
\]

### 2.3 結構結果與分支結果分離

原算子在未干預下先產生：

\[
r_i^{struct}=F_i(Pa_i).
\]

干預後結果則是：

\[
r_i^{branch}=I_i(r_i^{struct}).
\]

v0.7 因此新增 `structural_result`，避免用原算子不變式直接否定合法的軟干預結果。若不分開這兩層，任何 \(a\neq1\) 或 \(b\neq0\) 的軟干預都會被舊版局部稽核錯判為算術錯誤。

---

## 3. 固定點循環內的干預重求解

### 3.1 基線循環

考慮：

\[
x=0.5y+1,
\]

\[
y=0.5x+1.
\]

其固定點為：

\[
x=y=2.
\]

v0.5 已能對文件明示的 Jacobi 群執行同步迭代，但 v0.6 禁止直接干預群組成員。原因是：循環成員不能像 DAG 節點一樣只重算一次。

### 3.2 循環內硬干預

執行：

\[
do(x=4)
\]

後，方程組成為：

\[
x=4,
\]

\[
y=0.5x+1.
\]

Runtime 在每次 Jacobi 迭代中鎖定 \(x=4\)，只更新其他成員，得到：

\[
y=3.
\]

並記錄切斷：

\[
y\rightarrow x.
\]

### 3.3 循環內軟干預

對 \(x\) 執行 `soft_shift(+1)`：

\[
x=(0.5y+1)+1=0.5y+2,
\]

\[
y=0.5x+1.
\]

解析解為：

\[
x=\frac{10}{3},
\qquad
 y=\frac{8}{3}.
\]

Runtime 將變換後方程納入每輪迭代，重新求解整個固定點群，而不是只修改一次輸出。

### 3.4 適用邊界

目前只支援：

- 文件明示的固定點群；
- `jacobi` 同步迭代；
- 已知結構方程；
- 可在迭代上限與容忍度內收斂的系統。

因此：

\[
\boxed{\text{數值收斂案例通過}\neq\text{任意循環皆可求解}}
\]

---

## 4. 分支差分帳

### 4.1 目的

只保存分支最終結果不足以回答：

- 哪些交易改變；
- 改變是目標干預還是後代反應；
- 基線與分支狀態是否相同；
- 重跑是否產生同一差分證據。

v0.7 因此新增：

```text
MMLC-BRANCH-DIFF v0.7
```

### 4.2 差分紀錄

每筆紀錄至少包含：

\[
(tx_i,
 v_i^{base},
 v_i^{branch},
 \Delta_i,
 s_i^{base},
 s_i^{branch},
 role_i).
\]

角色包括：

- `hard_intervention`；
- `soft_intervention`；
- `fixed_point_response`；
- `descendant_response`；
- `unchanged`。

### 4.3 雜湊鏈

差分帳依穩定交易順序建立：

\[
h_k=
SHA256(h_{k-1}\Vert canonical(record_k)).
\]

這可檢查：

- 紀錄順序是否被改動；
- 中間差分是否被替換；
- 相同輸入重跑是否得到相同 head hash。

但它不是：

- 區塊鏈；
- 分散式共識；
- 外部可信時間戳；
- 不可否認的法律證據。

---

## 5. 干預衝突與冗餘稽核

### 5.1 衝突

同一語境中：

```yaml
- {kind: do_set, target_tx_id: root, value: 4}
- {kind: do_set, target_tx_id: root, value: 5}
```

兩者無法同時成立，因此分支被標記：

```text
status = CONFLICT
```

v0.7 不會任意採用「最後一筆覆蓋前一筆」，也不讓該分支拖垮其他語境。整體分析可為 `PARTIAL`，有效分支繼續完成。

### 5.2 等價冗餘

若兩筆干預正規化後完全等價，則標記：

```text
status = WARN
redundancy = true
```

Runtime 只執行第一筆，但保留重複宣告的稽核資訊。

### 5.3 尚未定義的組合

本版沒有自動定義：

- 同目標硬干預與軟干預的先後級；
- 多個非等價軟干預的組合順序；
- 非交換干預的代數。

因此未明示組合規則時，同一目標的非等價干預全部視為衝突。這是保守策略，不是假裝任意組合都具有唯一語義。

---

## 6. 有限觀測可識別性

### 6.1 定義

給定使用者宣告的觀測交易集合：

\[
O=\{o_1,\ldots,o_m\},
\]

對每個語境 \(c\) 建立簽章：

\[
\sigma_O(c)=
(v_{o_1}^{c},\ldots,v_{o_m}^{c}).
\]

若：

\[
\sigma_O(c_1)=\sigma_O(c_2),
\]

則在該有限觀測集合下，兩分支不可區分，並被放入同一等價類。

### 6.2 分類

Runtime 輸出：

- `DISTINGUISHABLE`：相對基線可見且簽章唯一；
- `EFFECT_VISIBLE_CONTEXT_NOT_UNIQUE`：效應可見，但至少另一語境具有相同簽章；
- `NOT_DISTINGUISHABLE`：觀測簽章與基線相同；
- `CONFLICT`：干預集合衝突，未執行；
- `BASELINE`：基線語境。

### 6.3 範例

在只觀測 `leaf` 時：

\[
do(root=4)
\]

與：

\[
soft\_shift(root,+1)
\]

可能都導致：

\[
leaf=12.
\]

因此：

\[
\sigma_{\{leaf\}}(c_1)
=
\sigma_{\{leaf\}}(c_2).
\]

Runtime 保留等價類，而不宣稱知道發生了哪一種機制。

### 6.4 精確邊界

本版「可識別性」只表示：

> 在已知決定性模型、已執行分支與指定觀測格上，輸出簽章是否足以區分語境。

它不表示：

- Pearl 意義下的一般因果可識別性；
- 從觀測分布推導干預分布；
- 潛在混雜已被排除；
- 真實因果圖已由資料識別；
- 在其他未觀測變數上兩機制仍等價。

---

## 7. 實作架構

本版主要改動：

| 模組 | v0.7 職責 |
|---|---|
| `mmlc/fdcs.py` | 干預正規化、衝突／冗餘、軟干預、差分帳、識別性 |
| `mmlc/runtime.py` | 硬／軟干預執行、固定點群重求解、衝突分支隔離 |
| `mmlc/types.py` | `structural_result` 與 `intervention_kinds` |
| `mmlc/report.py` | E7 分支、衝突、差分帳與識別性報告 |
| `mmlc/parser.py` | MMLF v0.7 schema 載入 |
| `schemas/mmlf-v0.7.schema.json` | 軟干預與觀測交易格式 |

新增範例：

- `fdcs_soft_interventions.yaml`；
- `fdcs_fixed_point_interventions.yaml`；
- `fdcs_conflict_identifiability.yaml`。

新增測試：

- `test_e7_soft_cyclic_diff_identifiability.py`。

---

## 8. 測試與實驗結果

### 8.1 自動測試

\[
\boxed{49\text{ 項測試全部通過}}
\]

新增 E7 測試涵蓋：

1. 軟干預保留傳入邊；
2. 仿射轉換結果與後代重算；
3. 固定點群內硬干預；
4. 固定點群內軟干預；
5. 分支差分雜湊鏈；
6. 衝突分支隔離與冗餘；
7. 有限觀測等價類。

### 8.2 軟干預壓力測試

| 指標 | 結果 |
|---|---:|
| 隨機帳本 | 64 |
| 每帳本交易數 | 32 |
| 交易值檢查 | 2,048 |
| 值錯誤 | 0 |
| 非預期切邊 | 0 |
| 傳入邊保留錯誤 | 0 |
| Accuracy | 1.0 |

### 8.3 固定點干預

| 指標 | 結果 |
|---|---:|
| 隨機收縮系統 | 64 |
| 硬／軟分支 | 128 |
| 解析值檢查 | 256 |
| 值錯誤 | 0 |
| 收斂錯誤 | 0 |
| 切邊契約錯誤 | 0 |
| Accuracy | 1.0 |

### 8.4 差分帳

| 指標 | 結果 |
|---|---:|
| 雜湊鏈錯誤 | 0 |
| 決定性重跑錯誤 | 0 |
| Accuracy | 1.0 |

### 8.5 衝突與冗餘

| 指標 | 結果 |
|---|---:|
| 案例 | 256 |
| 衝突真陽性 | 128 |
| 衝突誤報 | 0 |
| 冗餘真陽性 | 128 |
| 冗餘誤報 | 0 |
| Accuracy | 1.0 |

### 8.6 有限觀測識別性

| 指標 | 結果 |
|---|---:|
| 帳本 | 64 |
| 等價類錯誤 | 0 |
| 不可見效應錯誤 | 0 |
| 衝突隔離錯誤 | 0 |
| Accuracy | 1.0 |

本次 E7 隨機實驗耗時約 1.92 秒；此數值只描述當前容器、資料規模與實作版本，不外推為通用效能承諾。

---

## 9. 開發過程中修正的問題

### 9.1 軟干預被原算子稽核誤判

初始設計直接以干預後結果接受原算子稽核，等價於要求：

\[
aF(x)+b=F(x),
\]

這會使所有非恆等軟干預失敗。修正方式是分離 `structural_result` 與最終分支結果，並由干預契約驗證兩者關係。

### 9.2 固定點干預不能套用 DAG 單次重算

循環成員若只在拓撲執行後覆寫一次，其他成員不會達到新固定點。修正後，硬干預在每輪迭代鎖定目標值，軟干預在每輪套用變換，直到整群收斂或明示失敗。

### 9.3 衝突語境不應使整份帳本失敗

多語境研究中，一個錯誤分支若直接拋出全域例外，會失去其他合法反事實。v0.7 改為分支級 `CONFLICT`，並在總體標記 `PARTIAL`。

### 9.4 基線與不可見干預的語義區分

基線本身不是「相對自己可識別的干預」。識別性報告因此將基線標為 `BASELINE`，並允許恆等軟干預與基線形成同一觀測等價類。

---

## 10. 可否證條件

以下任一情況出現，即表示 E7 實作主張失敗：

1. 軟干預造成未宣告的傳入切邊；
2. 干預後結果不符合 \(aF+b\)；
3. 循環內干預只修改目標而未重求其他群組成員；
4. 固定點不收斂卻被標為成功；
5. 不等價的同目標干預被靜默覆蓋；
6. 衝突分支污染其他有效分支；
7. 相同分支重跑產生不同差分 head hash；
8. 相同觀測簽章被宣稱為唯一可識別；
9. MMLF v0.1–v0.6 文件因升級而偷偷獲得 v0.7 副作用。

目前設計實驗中未觀察到上述失敗。

---

## 11. 現階段結論

MMLC 的技術鏈已推進為：

\[
\text{格級計算}
\rightarrow
\text{多層稽核}
\rightarrow
\text{符號交換}
\rightarrow
\text{根因追蹤}
\rightarrow
\text{方向執行}
\rightarrow
\text{跨軸修復}
\rightarrow
\text{時間與固定點}
\rightarrow
\text{硬式反事實}
\rightarrow
\text{軟干預與循環重求解}
\rightarrow
\text{差分與有限觀測等價類}.
\]

本版支持以下有限結論：

\[
\boxed{
\text{MMLC 已具備第一版硬／軟干預並存、可重新求解循環且可稽核分支差異的決定性帳本。}
}
\]

但尚不能宣稱：

- 能從現實資料自動發現因果；
- 完成一般統計可識別性；
- 支援任意非線性或混沌循環；
- 支援任意軟干預函數或機率分布變換；
- 差分雜湊鏈等於分散式信任；
- 有限觀測等價類在所有觀測域中仍成立。

下一階段可進入 MMLC Runtime v0.8／E8：機率分支、不確定性傳播、干預成本與政策選擇，以及對不可識別分支生成最小追加觀測集合。
