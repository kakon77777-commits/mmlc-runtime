# MMLC Runtime v0.9：連續近似、相關不確定性、資訊價值與序貫決策

**文件編號：** EML-MMLC-2026-R09-E9  
**Runtime：** MMLC Runtime 0.9.0  
**MMLF：** v0.1–v0.9  
**日期：** 2026-07-23  
**作者：** Neo.K with Aletheia（GPT-5.6 Thinking）  
**組織：** EveMissLab

---

## 摘要

MMLC Runtime v0.8 已能對有限、離散、由文件明示的反事實分支附加機率，傳播交易級不確定性，依期望效用、風險與成本排序政策，並搜尋最小追加觀測集合。v0.9／E9 進一步處理四個尚未閉合的問題：

1. 如何把連續分布轉換成仍可執行、可稽核、可重跑的有限帳本分支；
2. 如何保存多個不確定變數之間的相關結構；
3. 如何區分「最少觀測格」與「最低觀測成本」；
4. 如何計算觀測的資訊價值，並在有限時域內選擇「先觀測、再決策」的序貫策略。

v0.9 實作決定性 antithetic Halton 樣本、Gaussian copula、Cholesky 相關轉換、四種連續邊際、經驗矩稽核、成本感知 hitting-set、單步期望樣本資訊價值，以及最多三階的有限情境動態規劃。

正式 E9 實驗包含：

- 32 個隨機連續相關帳本；
- 8,192 條決定性近似分支；
- 32 個非法相關矩陣負對照；
- 32 組資訊價值與兩階段序貫決策；
- 64 組成本感知追加觀測；
- 64 項自動測試；
- E0–E9 全階段回歸。

本次結果全部通過。但本報告不宣稱 MMLC 已完成解析連續機率推論、一般 Bayesian inference、分布學習或真實世界因果識別。v0.9 的連續性仍由有限決定性分支近似，所有分布、相關、機率、效用與成本都由文件明示。

---

## 1. v0.8 的缺口

v0.8 的不確定性具有精確但有限的形式：

\[
\Omega=\{\omega_1,\ldots,\omega_n\},
\qquad
\sum_i P(\omega_i)=1.
\]

這適合：

- 明確枚舉的市場情境；
- 少量政策結果；
- 已知反事實分支；
- 精確離散風險。

但若輸入被描述為：

\[
X\sim\mathcal N(\mu,\sigma^2),
\]

或兩個變數具有：

\[
\operatorname{Corr}(X,Y)=\rho,
\]

v0.8 必須由使用者手工展開大量情境，且沒有一致的採樣、相關轉換與近似誤差報告。

另一方面，v0.8 的追加觀測只最小化觀測格數：

\[
\min |S|.
\]

它尚未處理：

\[
\min \sum_{x\in S}c_x,
\]

也沒有回答觀測是否值得付出成本，以及應該一次觀測全部訊號，還是根據第一個觀測結果再決定第二步。

---

## 2. E9 設計原則

### 2.1 可稽核有限近似

v0.9 不直接把「連續分布」包裝成不可檢查的黑箱物件，而是將其編譯成有限分支：

\[
\mathcal D
\longrightarrow
\{(\omega_i,p_i)\}_{i=1}^{N}.
\]

每個分支仍是普通 MMLC／FDCS 語境，因此可接受：

- 干預衝突稽核；
- 依賴與切邊稽核；
- 固定點重新求解；
- 機率質量稽核；
- 分支差分帳；
- semantic hash；
- 決定性重跑。

### 2.2 近似與解析分離

Runtime 同時保存：

- 宣告分布；
- 宣告相關矩陣；
- 採樣方法；
- 樣本數；
- 經驗均值；
- 經驗標準差；
- 經驗共變異數；
- 經驗相關矩陣。

因此：

\[
\text{Target moments}
\neq
\text{Empirical moments}.
\]

兩者接近是有限近似的證據，不是解析相等證明。

### 2.3 決策假設必須明示

資訊價值只在以下條件下計算：

- 所有政策共享相同 `scenario_id` 集合；
- 同一情境在各政策下具有相同先驗機率；
- 候選觀測在政策執行前可取得；
- 同一情境中的候選觀測值不依政策而改變；
- 效用、政策成本與觀測成本皆由文件明示。

若候選訊號受政策影響，它是政策後結果，不是事前訊號，Runtime 會拒絕將其用於 observe-then-act 分析。

---

## 3. 連續分布的決定性近似

### 3.1 Halton 序列

第 \(d\) 維使用不同質數底數 \(b_d\)，產生低差異序列：

\[
u_{n,d}=\operatorname{Halton}(n,b_d).
\]

為降低一階矩漂移，v0.9 使用反對稱點：

\[
\mathbf u_n,
\qquad
1-\mathbf u_n.
\]

這對常態與均勻邊際可使樣本在中心附近保持對稱。

### 3.2 高斯 copula

先將每個均勻分量轉成標準常態分數：

\[
z_d=\Phi^{-1}(u_d).
\]

給定宣告相關矩陣：

\[
R=LL^\top,
\]

其中 \(L\) 由 Cholesky 分解取得，相關常態分數為：

\[
\mathbf z^{\mathrm{corr}}=L\mathbf z.
\]

再轉回相關均勻分量：

\[
q_d=\Phi(z_d^{\mathrm{corr}}).
\]

最後由各邊際反累積函數得到：

\[
x_d=F_d^{-1}(q_d).
\]

### 3.3 支援邊際

v0.9 支援：

1. 常態分布：

\[
X=\mu+\sigma z.
\]

2. 均勻分布：

\[
X=a+q(b-a).
\]

3. 三角分布：分段反 CDF；

4. 對數常態分布：

\[
X=\exp(\mu+\sigma z).
\]

### 3.4 相關矩陣稽核

矩陣必須滿足：

\[
R=R^\top,
\]

\[
R_{ii}=1,
\]

\[
-1\le R_{ij}\le1,
\]

並在目前實作下可完成嚴格正定 Cholesky 分解。

半正定但奇異的相關矩陣目前不接受，避免以數值抖動偷偷修改使用者宣告。

---

## 4. 連續樣本如何進入 MMLC

每一個樣本被編譯成一個普通 FDCS 語境：

```yaml
id: correlated-shocks-sample-0042
policy_id: uncertainty-only
scenario_id: correlated-shocks-0042
probability: 0.00390625
interventions:
  - kind: do_set
    target_tx_id: shock_a
    value: ...
  - kind: do_set
    target_tx_id: shock_b
    value: ...
```

因此：

\[
\sum_{i=1}^{N}p_i=1,
\qquad
p_i=\frac1N.
\]

連續變數目前必須指向不同交易。若兩個變數同時指向同一交易，Runtime 無法知道它們應相加、相乘、覆寫或組合，因此明示拒絕，而不是任意決定。

---

## 5. 相關不確定性稽核

對樣本 \(x_i^{(k)}\)，經驗均值：

\[
\hat\mu_i=\frac1N\sum_{k=1}^{N}x_i^{(k)}.
\]

經驗共變異數：

\[
\widehat{\operatorname{Cov}}(X_i,X_j)
=
\frac1N\sum_{k=1}^{N}
(x_i^{(k)}-\hat\mu_i)
(x_j^{(k)}-\hat\mu_j).
\]

經驗相關係數：

\[
\hat\rho_{ij}
=
\frac{\widehat{\operatorname{Cov}}(X_i,X_j)}
{\hat\sigma_i\hat\sigma_j}.
\]

範例宣告：

\[
\mu_a=2,
\quad
\sigma_a=1.5,
\]

\[
\mu_b=-1,
\quad
\sigma_b=0.5,
\]

\[
\rho_{ab}=0.65.
\]

256 樣本的本次結果：

\[
\hat\mu_a=2,
\qquad
\hat\mu_b=-1,
\]

\[
\hat\sigma_a\approx1.48776,
\qquad
\hat\sigma_b\approx0.47617,
\]

\[
\hat\rho_{ab}\approx0.63754.
\]

這些差異是有限樣本誤差，報告會保留而不隱藏。

---

## 6. 投影支撐數不等於樣本數

第一輪測試曾錯誤要求：

\[
\text{support size}=\text{sample count}.
\]

但若下游交易為：

\[
Y=X_1+X_2,
\]

不同的二維樣本可能投影到相同 \(Y\) 值。因此：

\[
|\operatorname{Supp}(Y)|
\le N.
\]

256 個樣本在範例中形成 254 個不同下游輸出值，這不是樣本遺失，而是多維狀態經函數映射後的合法合併。

測試因此改為驗證：

- 原始生成語境數；
- 總機率質量；
- 下游期望與變異；
- 支撐機率加總；
- 決定性分析雜湊。

---

## 7. 成本感知追加觀測

v0.8 搜尋：

\[
\min |S|
\]

使每一組仍模糊的分支配對至少被一個觀測格區分。

v0.9 同時搜尋：

\[
\min \sum_{x\in S}c_x.
\]

範例中：

- 單格 \(z\) 可以一次區分所有分支，成本 1.0；
- \(x\) 與 \(y\) 必須共同觀測，總成本 0.4。

因此：

\[
S_{\min|S|}=\{z\},
\]

\[
S_{\min c}=\{x,y\}.
\]

v0.9 同時保留兩者，不把不同目標函數混成一個答案。

---

## 8. 單步資訊價值

令政策集合為 \(\Pi\)，情境集合為 \(\Omega\)，淨效用為：

\[
U_{\mathrm{net}}(\pi,\omega)
=
U(\pi,\omega)-\gamma C(\pi,\omega).
\]

觀測前最佳值：

\[
V_0
=
\max_{\pi\in\Pi}
\mathbb E[U_{\mathrm{net}}(\pi,\omega)].
\]

觀測訊號 \(Z\) 後：

\[
V_Z
=
\sum_z P(z)
\max_{\pi\in\Pi}
\mathbb E[U_{\mathrm{net}}(\pi,\omega)\mid Z=z].
\]

Gross EVSI：

\[
\operatorname{EVSI}_{\mathrm{gross}}(Z)=V_Z-V_0.
\]

Net EVSI：

\[
\operatorname{EVSI}_{\mathrm{net}}(Z)
=V_Z-V_0-C_Z.
\]

### 8.1 範例

四個等機率情境、四個政策。每個政策只在一個情境產生 12，其餘為 0。

觀測前：

\[
V_0=3.
\]

單一二元訊號將四個情境分成兩組，觀測後最佳期望值：

\[
V_Z=6.
\]

所以：

\[
\operatorname{EVSI}_{\mathrm{gross}}=3.
\]

觀測成本為 0.5：

\[
\operatorname{EVSI}_{\mathrm{net}}=2.5.
\]

---

## 9. 有限時域序貫決策

v0.9 將「立即選政策」視為停止動作，並在剩餘觀測候選與剩餘深度上使用動態規劃。

令 belief 由目前仍可能的情境子集與正規化機率構成：

\[
V(B,d)
=
\max
\left\{
V_{\mathrm{stop}}(B),
\max_{Z\in\mathcal Z}
\left[-C_Z+
\sum_zP(z\mid B)V(B_z,d-1)
\right]
\right\}.
\]

當：

\[
d=0,
\]

或觀測不再增加區分能力時，選擇當前期望淨效用最高的政策。

### 9.1 兩階段範例

兩個二元訊號共同識別四個情境。

第一階段觀測後仍剩兩個情境，第二階段再觀測另一訊號即可唯一識別。

完全識別後政策效用為 12，兩次觀測總成本為 1：

\[
V_{\mathrm{seq}}=12-1=11.
\]

相較觀測前：

\[
V_0=3,
\]

序貫淨資訊價值：

\[
11-3=8.
\]

若每次觀測成本提高到 20，Runtime 會選擇立即停止並直接選政策：

\[
V_{\mathrm{seq}}=V_0=3.
\]

這證明觀測不是預設必須執行的動作。

---

## 10. E9 隨機實驗

### 10.1 連續與相關不確定性

| 指標 | 結果 |
|---|---:|
| 隨機帳本 | 32 |
| 每帳本樣本 | 256 |
| 近似分支總數 | 8,192 |
| 均值錯誤 | 0 |
| 標準差錯誤 | 0 |
| 相關係數錯誤 | 0 |
| 機率質量錯誤 | 0 |
| 決定性雜湊錯誤 | 0 |
| 非法相關矩陣負對照 | 32／32 正確拒絕 |
| Accuracy | **1.0** |

測試容忍度：

- 標準差相對誤差：5%／8%；
- 相關係數絕對誤差：0.06；
- 均值與機率總和：接近機器精度。

第一版只使用 128 樣本時，部分低至中度負相關案例的誤差超過 0.06。這不是公式錯誤，而是有限低差異樣本仍不足以穩定重現所有隨機相關目標。正式 E9 因此提高為 256 樣本，並保留經驗誤差而非調整輸出迎合目標。

### 10.2 資訊價值與序貫決策

| 指標 | 結果 |
|---|---:|
| 案例 | 32 |
| 單步資訊價值錯誤 | 0 |
| 序貫策略錯誤 | 0 |
| 分析雜湊錯誤 | 0 |
| Accuracy | **1.0** |

### 10.3 成本感知觀測

| 指標 | 結果 |
|---|---:|
| 案例 | 64 |
| 錯誤 | 0 |
| 最小格數解 | `{z}` |
| 最低成本解 | `{x,y}` |
| Accuracy | **1.0** |

E9 本次獨立重跑耗時約 27.02 秒；外層程序計時約 28.93 秒。這是本次容器執行值，不是跨硬體效能承諾。

---

## 11. 完整回歸

目前共有：

\[
\boxed{64\text{ 項自動測試全部通過}}
\]

| 階段 | 回歸結果 |
|---|---|
| E0 | 算術、竄改定位、反抵消與決定性重跑通過 |
| E1 | 8,192／8,192 符號—數值交換通過 |
| E2 | 根因與污染 precision／recall 1.0 |
| E3 | 方向中立、方向敏感與固定空間引用通過 |
| E4 | 160／160 與獨立表格參考一致 |
| E5 | 時間、延遲、固定點與不可變補帳通過 |
| E6 | 硬干預、多語境與方向／分形權重通過 |
| E7 | 軟干預、循環重求解、衝突與有限可識別性通過 |
| E8 | 離散機率、政策排序與精確追加觀測通過 |
| E9 | 連續近似、相關不確定性、觀測成本、EVSI 與序貫決策通過 |

---

## 12. 目前不能宣稱的事情

v0.9 不能宣稱：

1. 連續分布已被解析求解；
2. 256 個樣本對所有尾端事件都足夠；
3. Gaussian copula 能描述所有非線性依賴；
4. 相關矩陣來自真實資料；
5. 分支機率已完成校準；
6. 資訊價值等於真實世界採集資訊的收益；
7. 序貫策略已處理部分可觀測 MDP；
8. 目前風險中立的後驗決策等於 v0.8 風險懲罰政策排序；
9. 觀測值含測量噪音；
10. 連續訊號已完成解析 Bayesian posterior；
11. 半正定奇異相關矩陣已支援；
12. MMLC 已優於專業 probabilistic programming 或 decision-analysis 框架。

---

## 13. 現階段技術判定

MMLC 的技術鏈已推進為：

\[
\text{矩陣計算}
\rightarrow
\text{多層稽核}
\rightarrow
\text{根因與修復}
\rightarrow
\text{方向與時間}
\rightarrow
\text{硬／軟反事實}
\rightarrow
\text{離散機率}
\rightarrow
\text{連續有限近似}
\rightarrow
\text{相關不確定性}
\rightarrow
\text{資訊價值}
\rightarrow
\text{序貫觀測與決策}.
\]

因此目前可以確認：

\[
\boxed{
\text{MMLC 已具備第一版可重現、可稽核的相關連續不確定性近似與有限時域觀測—決策閉環。}
}
\]

這個結論只適用於：

- 使用者明示模型；
- 有限交易圖；
- 有限樣本；
- Gaussian copula；
- 明示效用與成本；
- 最多三階觀測；
- 決定性執行與稽核。

---

## 14. 下一階段建議

下一階段可進入 MMLC Runtime v1.0／E10，但不宜繼續只增加功能。v0.1–v0.9 已形成完整實驗鏈，v1.0 應優先完成：

1. 格式與 API 穩定化；
2. 性能基準與記憶體基準；
3. 與試算表、DAG、SymPy、因果圖與 probabilistic programming 的統一對照；
4. 靜態型別與錯誤碼穩定；
5. 可重現測試資料集；
6. 版本遷移工具；
7. 安全與資源上限；
8. 公開技術白皮書與最小教學。

換言之，下一步更合理的方向是：

\[
\boxed{
\text{從連續擴張功能，轉向統合、穩定、基準、公開與可維護。}
}
\]
